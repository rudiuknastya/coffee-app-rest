secret-key: ${secretKey}
sender: ${sender}
upload:
  path: /home/avada/web/slj.avada-media-dev1.od.ua/projects/A.Rudiuk/Coffee-app

spring:
  mail:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/avada_slj_coffee
    username: ${usernameDB}
    password: ${passwordDB}
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

springdoc:
  swagger-ui:
    disable-swagger-default-url: true
    path: /